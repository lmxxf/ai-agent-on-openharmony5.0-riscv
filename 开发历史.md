# 开发历史

**你正在读这个文件。这是你之前做过的事。**

---

## 项目目标
在OpenHarmony Settings应用里加一个本地AI助手，用llama.cpp跑Qwen模型。

## 设备信息
- **已验证**: 进迭时空 RISC-V 平板 (Spacemit X60, 8核, 16GB) + OpenHarmony 5.0
- **待验证**: 紫光展锐 P7885 (4xA76+4xA55, 8T NPU) + OpenHarmony 6.0

---

## 已经做完的事

### 1. Settings编译环境 ✅
- `local.properties` 配置了SDK路径
- `build-profile.json5` 配置了签名
- 编译命令: `./hvigorw assembleHap --mode module -p product=default -p module=phone`

### 2. AI菜单和页面 ✅
- `settingList.ets` 加了AI助手入口（在"用户和账户"上面）
- `aiAssistant.ets` 聊天界面
- 图标: `ic_ai.svg`

### 3. llama.cpp交叉编译 ✅
- 编译脚本: `llama_cpp/build_riscv64.sh`
- 产物在: `llama_cpp/build_riscv64/bin/`
- 关键库: `libllama.so`, `libggml*.so`

### 4. 设备端验证 ✅
设备端命令行能跑：
```bash
hdc shell
cd /data/local/tmp
export LD_LIBRARY_PATH=/data/local/tmp
./llama-run -t 8 qwen2.5-0.5b-q4.gguf "你好"
# 输出: 你好！有什么可以帮助你的吗？
```

### 5. NAPI封装 ✅ (2025-12-13)
- `product/phone/src/main/cpp/llama_napi.cpp` - NAPI wrapper
- `build_napi.sh` - 编译脚本
- `product/phone/libs/riscv64/` - 预编译库

导出的接口：
- `loadModel(modelPath, nThreads)` - 加载模型
- `unloadModel()` - 卸载模型
- `isModelLoaded()` - 检查状态
- `generate(prompt, maxTokens)` - 生成文本
- `stopGeneration()` - 停止生成

### 6. 集成到aiAssistant.ets ✅ (2025-12-13)
- 页面启动时自动加载模型
- 使用 Qwen2.5 chat template 格式化 prompt
- 保留最近3轮对话历史
- 显示模型加载状态，支持重新加载

### 7. NAPI模块加载成功 ✅ (2025-12-13)
解决了一堆 .so 依赖问题：
- 用 `patchelf` 修改了所有 .so 的 SONAME（去掉版本号 .so.0 → .so）
- 用 `patchelf` 修改了所有 .so 的 NEEDED（依赖的库名）
- 用 `patchelf` 修改了所有 .so 的 RUNPATH（改成 $ORIGIN）
- hvigorw 读取 `product/phone/libs/riscv64/` 目录（不是 src/main/libs）

### 8. 模型加载成功 ✅ (2025-12-13)
解决了文件路径问题：
- 应用沙箱内的路径是 `/data/storage/el2/base/files/`
- 不是 root shell 看到的 `/data/app/el2/100/base/com.ohos.settings/files/`
- 模型文件需要复制到应用沙箱目录

---

### 9. 异步推理 ✅ (2025-12-13)
解决了输入闪退问题：
- **根本原因**：`generate()` 同步调用阻塞 UI 线程超过 6 秒，系统检测到 ANR 后杀掉应用
- **解决方案**：用 NAPI 的 `napi_create_async_work` 把推理放到后台线程
- `llama_napi.cpp` 的 `Generate()` 改为返回 `Promise<string>`
- `aiAssistant.ets` 用 `.then()/.catch()` 处理异步结果
- 现在可以正常对话了

---

## 性能基线

### Spacemit X60 (RISC-V, 8核, 16GB)
| 模型 | 量化 | 速度 |
|------|------|------|
| Qwen2.5-0.5B | Q4_K_M | ~1-2 token/s |

### P7885 (ARM64, 4xA76+4xA55) - 预估
| 模型 | 量化 | 速度 |
|------|------|------|
| Qwen2.5-0.5B | Q4_K_M | ~5-10 token/s (预估) |

**说明：**
- Spacemit X60：CLI 和 NAPI 速度一致，不是代码问题。没有 RVV 向量扩展，只能标量运算，1-2 token/s 是硬件极限
- P7885：有 NEON 向量指令，llama.cpp 原生支持，预估快 5 倍。另有 8T NPU 可进一步加速（需展锐 SDK）

---

## 文件位置速查

| 文件 | 用途 |
|------|------|
| `llama_cpp/build_riscv64.sh` | llama.cpp 编译脚本 |
| `llama_cpp/build_riscv64/bin/` | llama.cpp 编译产物 |
| `build_napi.sh` | NAPI wrapper 编译脚本 |
| `product/phone/libs/riscv64/` | HAP 打包的预编译库 |
| `product/phone/src/main/cpp/llama_napi.cpp` | NAPI 封装 |
| `product/phone/src/main/ets/pages/aiAssistant.ets` | AI 页面 |

---

## 常用命令

```bash
# 编译 NAPI wrapper
./build_napi.sh

# patchelf 修复依赖（每次重新编译 NAPI 后都要做）
patchelf --replace-needed libllama.so.0 libllama.so product/phone/libs/riscv64/libllama_napi.so
patchelf --replace-needed libggml.so.0 libggml.so product/phone/libs/riscv64/libllama_napi.so
patchelf --replace-needed libggml-base.so.0 libggml-base.so product/phone/libs/riscv64/libllama_napi.so
patchelf --replace-needed libggml-cpu.so.0 libggml-cpu.so product/phone/libs/riscv64/libllama_napi.so

# 编译 HAP
rm -rf product/phone/build
./build_settings.sh

# 安装
hdc install product/phone/build/default/outputs/default/phone-default-signed.hap

# 查看日志
hdc shell hilog | grep -i "llama"

# 模型位置（应用沙箱内路径）
/data/storage/el2/base/files/qwen2.5-0.5b-q4.gguf
# 对应的物理路径（root shell）
/data/app/el2/100/base/com.ohos.settings/files/qwen2.5-0.5b-q4.gguf
```

---

## 踩过的坑

1. **hvigorw 不支持 riscv64 native 编译** - 只能手动编译 .so 再打包
2. **hvigorw 读取 `product/phone/libs/` 不是 `src/main/libs/`**
3. **.so 文件的 SONAME 带版本号** - 需要 patchelf 改掉
4. **.so 文件的 NEEDED 带版本号** - 需要 patchelf 改掉
5. **.so 文件的 RUNPATH 是编译机路径** - 需要 patchelf 改成 $ORIGIN
6. **hvigorw 有缓存** - 要 `rm -rf product/phone/build` 才能真正重新打包
7. **应用沙箱路径不同** - 应用进程看到的是 `/data/storage/el2/base/files/`，不是 root shell 看到的 `/data/app/el2/.../files/`
8. **build_napi.sh 输出目录要对** - 必须输出到 `product/phone/libs/riscv64/`
9. **同步调用导致 ANR** - NAPI 函数如果阻塞超过 6 秒，系统会杀掉应用。必须用 `napi_create_async_work` 做异步
