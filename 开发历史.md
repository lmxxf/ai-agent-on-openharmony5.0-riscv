# 开发历史

**你正在读这个文件。这是你之前做过的事。**

---

## 项目目标
在OpenHarmony Settings应用里加一个本地AI助手，用llama.cpp跑Qwen模型。

## 设备信息
- **当前测试**: 进迭时空 RISC-V 平板 (Spacemit X60, 8核, 16GB)
- **之后要做**: 公司ARM设备 + OpenHarmony 6.0

---

## 已经做完的事

### 1. Settings编译环境 ✅
- `local.properties` 配置了SDK路径
- `build-profile.json5` 配置了签名
- 编译命令: `./hvigorw assembleHap --mode module -p product=default -p module=phone`

### 2. AI菜单和页面 ✅
- `settingList.ets` 加了AI助手入口（在"用户和账户"上面）
- `aiAssistant.ets` 聊天界面（目前是模拟响应）
- 图标: `ic_ai.svg`

### 3. llama.cpp交叉编译 ✅
- 编译脚本: `llama_cpp/build_riscv64.sh`
- 产物在: `llama_cpp/build_riscv64/bin/`
- 关键库: `libllama.so`, `libggml*.so`

### 4. 设备端验证 ✅
Soul手动跑了一遍，确认能用：
```bash
hdc shell
cd /data/local/tmp
export LD_LIBRARY_PATH=/data/local/tmp
./llama-run -t 8 qwen2.5-0.5b-q4.gguf "你好"
# 输出: 你好！有什么可以帮助你的吗？
```

---

## 还没做的事

### 5. NAPI封装
把llama.cpp封装成ArkTS可调用的接口。需要：
- 写C++ NAPI代码
- 改CMake/BUILD.gn
- 导出 `loadModel()`, `generate()`, `unloadModel()`

### 6. 集成到aiAssistant.ets
把模拟响应换成真实的llama.cpp推理。

### 7. ARM适配
到时候再说。可能用MindSpore Lite + NPU。

---

## 文件位置速查

| 文件 | 用途 |
|------|------|
| `llama_cpp/build_riscv64.sh` | 编译脚本 |
| `llama_cpp/build_riscv64/bin/` | 编译产物 |
| `models/qwen2.5-0.5b-q4.gguf` | 测试模型(470MB) |
| `product/phone/src/main/ets/pages/aiAssistant.ets` | AI页面 |
| `product/phone/src/main/ets/pages/settingList.ets` | 主列表 |

---

## 常用命令

```bash
# 编译
./hvigorw assembleHap --mode module -p product=default -p module=phone

# 安装
hdc install product/phone/build/default/outputs/default/phone-default-signed.hap

# 启动Settings
hdc shell aa start -a com.ohos.settings.MainAbility -b com.ohos.settings
```

---

**下一步：写NAPI封装，让aiAssistant.ets能调用llama.cpp。**
